package com.meetingtotasks.app;

import com.getcapacitor.BridgeActivity;

/**
 * Capacitor bridge activity. Hosts the WebView that loads the Meeting-to-Tasks
 * web app (remote URL in production, or the bundled static export).
 */
public class MainActivity extends BridgeActivity {}
